# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=person_crud_db
DB_PORT=3306

# Server Configuration
PORT=5000

# CORS Configuration
# CORS_ALLOWED_ORIGINS can be a comma-separated list (e.g., http://localhost:3000,https://myapp.com)
CORS_ALLOWED_ORIGINS=http://localhost:3000
CORS_CREDENTIALS=true

# Instructions:
# 1. Copy this file and rename it to .env
# 2. Replace 'your_mysql_password' with your actual MySQL password
# 3. Adjust other values if needed (e.g., if MySQL runs on a different port)
# 4. Update FRONTEND_URL if your frontend runs on a different port or domain
